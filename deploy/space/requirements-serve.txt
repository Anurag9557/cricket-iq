# Serve-only dependencies for the live demo.
# Deliberately excludes torch / openai / langgraph / matplotlib / pytest — those are for
# offline training, commentary generation, and dev, none of which run in the request path.
fastapi
uvicorn[standard]
polars==1.42.1
pyarrow==25.0.0
numpy==2.5.1
scikit-learn==1.9.0
lightgbm==4.7.0
joblib==1.5.3
